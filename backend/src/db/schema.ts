// Drizzle-Schema-Datei — bewusst leer.
//
// Wir nutzen Drizzle Kit nur als Migration-Runner (siehe ADR-13).
// SQL-Schemas leben in src/db/migrations/*.sql, nicht in TypeScript.
//
// Bei Bedarf später (z.B. wenn Drizzle ORM für Queries eingeführt wird):
// Tabellen-Definitionen hier ergänzen.
export {};
