# Backend-Skeleton — Fastify + TypeScript + Auth + Swagger

> **Issue:** TT-02 | **Erstellt:** 2026-05-16 | **Status:** Draft

## Agent-Pattern

- [x] **Solo**

**Gewähltes Pattern:** Solo
**Begründung:** Klar abgegrenzter Backend-Bootstrap, lineare Tasks.
**Team-Komposition:** n/a

## Why

Grundgerüst für alle weiteren API-Endpoints. Auth + Multi-Tenant-Context muss von Anfang stehen — sonst funktioniert RLS nicht und alle nachfolgenden Issues bauen auf falschen Annahmen.

## What

Fastify 5+ Backend mit TypeScript, zwei DB-Pools (owner/app), JWT-Auth-Middleware, `withTenant()`-Helper, Swagger/OpenAPI, CORS, Rate-Limiting, Zod-Validation-Helper, Pino-Logger, strukturierte Error-Responses.

## Constraints

### Must
- Fastify 5+ mit `@fastify/cors`, `@fastify/rate-limit`, `@fastify/swagger`, `@fastify/swagger-ui`, `@fastify/jwt`
- Zwei Connection-Pools: `ownerPool` (für Migrations/Admin), `appPool` (alle API-Requests, RLS erzwungen)
- `withTenant(tenantId, fn)` Helper: `SET app.current_tenant_id = $1` + Function ausführen
- JWT-Middleware: Token validieren, `{userId, tenantId, role, isSuperAdmin}` extrahieren
- Login-Endpoint mit bcryptjs (Login-Logik inkl. FAILED_LOGIN_COUNT)
- Rate-Limit: 200 req/min global, 10 req/min auf `/api/auth/*`
- Health-Check `/api/health` → `{ status, db }`
- Swagger UI unter `/api/docs`
- `tsconfig.json` mit `strict: true`
- JWT_SECRET aus `.env`, mind. 64 Zeichen, Server startet nicht ohne

### Must Not
- Kein eigenes Auth-Library — Standard `@fastify/jwt`
- Keine CRUD-Endpoints (kommen in TT-03+)

### Out of Scope
- Refresh-Token-Rotation (Welle 3+)
- Passwort-Reset-Flow (Welle 3+)

## Current State

**Relevante Dateien:**
- `eslint.config.mjs`, `tsconfig.json` (aus Bootstrap)
- DB aus TT-01a–e

**Architektur-Dimensionen:** Security (JWT, Rate-Limit), Reliability (Health-Check), Maintainability (TS strict)

## Tasks

### T1: Backend-Projekt-Struktur
- [ ] `backend/package.json` mit Fastify-Dependencies
- [ ] `backend/src/server.ts` Boot-File
- [ ] `backend/src/db/pools.ts` (owner + app)
- [ ] `backend/src/db/withTenant.ts` Helper

### T2: Auth-Middleware
- [ ] `backend/src/auth/jwt.ts` — Token erstellen + verifizieren
- [ ] `backend/src/auth/middleware.ts` — preHandler für JWT-Check
- [ ] `backend/src/auth/password.ts` — bcryptjs hash + compare (cost 12)

### T3: Login + Health-Endpoint
- [ ] `POST /api/auth/login` — bcrypt-Compare, FAILED_LOGIN_COUNT-Logik, JWT-Issue
- [ ] `GET /api/health` — DB-Ping
- [ ] Tests für beide

### T4: Swagger + CORS + Rate-Limit
- [ ] Plugin-Registrierung in `server.ts`
- [ ] CORS-Whitelist aus `.env.CORS_ORIGINS`
- [ ] Rate-Limit-Config

### T5: Validation-Helper + Error-Handler
- [ ] `backend/src/lib/validation.ts` mit `validateBody(schema)` Higher-Order
- [ ] Globaler Error-Handler: `{ error: { code, message } }`

### T_last: Dokumentation + Config
- [ ] `ARCHITECTURE_DESIGN.md §9` ergänzen
- [ ] `INDEX.md` + `COMPONENT_INVENTORY.md`
- [ ] `Components/backend.md` + `Components/auth.md` Phase-Status auf "active"
- [ ] CHANGELOG.md, VERSION 0.2.0 + sync

## Abhängigkeiten

- **Blockiert durch:** TT-01e
- **Blockiert:** Alle TT-03 bis TT-27

## Acceptance Criteria

- [ ] `npm run dev` startet Backend auf Port 3000
- [ ] `GET /api/health` → 200 mit `{status:"ok", db:"connected"}`
- [ ] `POST /api/auth/login` mit Pilot-User → JWT zurück
- [ ] `GET /api/docs` zeigt Swagger UI
- [ ] Falsches Passwort → FAILED_LOGIN_COUNT erhöht, nach 5x LOCKED_UNTIL gesetzt
- [ ] Vitest: ≥8 Tests (Login, Health, Rate-Limit, Error-Handler)
- [ ] spec-gate.sh + doc-version-sync.sh + orphan-check.sh grün
