# Tenants + Users CRUD (inkl. 6 Rollen)

> **Issue:** TT-03 | **Erstellt:** 2026-05-16 | **Status:** Draft

## Agent-Pattern

- [x] **Solo**

**Gewähltes Pattern:** Solo
**Begründung:** Klar abgegrenzte CRUD, baut auf TT-02 auf.
**Team-Komposition:** n/a

## Why

Super-Admin muss neue Tenants anlegen können (Franchise-Onboarding). Admins müssen User verwalten (Planer, Vorarbeiter, Mitarbeiter). Ohne User-CRUD kein produktiver Betrieb.

## What

CRUD für TENANTS + USERS. Endpoints: `GET/PUT /api/tenants`, `GET/POST/PUT/DELETE /api/users`, `POST /api/users/:id/change-password`. Rollen-basierte Authorization (siehe ADR-09 Matrix).

## Constraints

### Must
- Routen: `tenants.ts`, `users.ts`
- Tenant-CRUD nur für SUPER_ADMIN (Cross-Tenant) oder ADMIN (eigener Tenant)
- User-Create nur ADMIN
- User-Self-Update für eigene User-ID
- Passwort-Komplexität: ≥8 Zeichen, ≥1 Großbuchstabe, ≥1 Zahl (Zod)
- bcryptjs cost 12
- `DELETE /api/users/:id` Soft-Delete, nicht self-delete
- Frontend-Seiten für Tenant- + User-Verwaltung

### Must Not
- Klartext-Passwörter loggen
- Selbst-Löschen erlauben

## Current State

**Relevante Dateien:** Auth-Middleware aus TT-02, USERS-Schema aus TT-01a.

**Architektur-Dimensionen:** Security, Privacy/DSGVO

## Tasks

### T1: Tenant-Endpoints
- [ ] `GET /api/tenants` (eigener oder alle für Super-Admin)
- [ ] `PUT /api/tenants/:id` (ADMIN eigenes, SUPER_ADMIN alle)

### T2: User-Endpoints
- [ ] CRUD-Endpoints + Authorization-Check
- [ ] Change-Password mit altem Passwort als Verify

### T3: Frontend
- [ ] `pages/UsersPage.tsx` (Tabelle + Form)
- [ ] `pages/TenantSettingsPage.tsx`

### T4: Tests
- [ ] EMPLOYEE darf keinen User anlegen (403)
- [ ] Passwort wird gehasht
- [ ] Soft-Delete verhindert Self-Delete

### T_last
- [ ] Doku-Sync, VERSION 0.2.1

## Abhängigkeiten

- **Blockiert durch:** TT-02
- **Blockiert:** TT-06 (Employees referenzieren USERS)

## Acceptance Criteria

- [ ] CRUD funktioniert end-to-end
- [ ] Authorization-Matrix umgesetzt
- [ ] Vitest: ≥6 Tests
- [ ] spec-gate.sh + doc-version-sync.sh + orphan-check.sh grün
