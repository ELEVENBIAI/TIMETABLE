# Plan-Generator — Woche aus Template erzeugen (MVP-CUT)

> **Issue:** TT-18 | **Erstellt:** 2026-05-16 | **Status:** Draft

## Agent-Pattern

- [ ] Solo
- [ ] Subagent
- [x] **Agent-Team** — Cross-Layer (Backend-Service + Frontend + Tests), mehrere Sub-Tasks
- [ ] Parallel-Subagents

**Gewähltes Pattern:** Agent-Team
**Begründung:** Plan-Generator orchestriert mehrere Services (frequency-engine, absence-loader, conflict-checker), hat Backend- + Frontend-Anteil, braucht umfangreiche Tests. Trigger-Bedingung: >3 unabhängige Tasks Cross-Layer.
**Team-Komposition:** Lead (Sonnet) für Orchestrierung + Explore (Haiku) für DB-Query-Recherche + Plan (Sonnet) für Algorithmus-Verifikation

## Why

Der MVP-Abschluss-Stein. Robert drückt "Woche generieren" und bekommt einen vorbefüllten DRAFT-Plan in 5 Sekunden mit Warnings. Statt 3 Stunden Excel = 5 Minuten Review.

## What

Backend-Service `schedule-generator.ts` + API-Endpoint `POST /api/schedules/generate` + Frontend-Button mit Warnings-Dialog.

## Constraints

### Must
- Input: `{ template_id, week_start }`
- Output: `{ schedule, entries, warnings[] }`
- Generator-Schritte:
  1. Template-Entries laden
  2. Frequenz-Engine: Welche Leistungen fällig in KW?
  3. WASTE-Schedules: Raus+Rein-Entries einfügen
  4. Absences-Check: REASSIGNMENT_NEEDED markieren
  5. Verfügbarkeits-Check (EMPLOYEE_AVAILABILITY)
  6. Auslastungs-Check (Stunden vs WEEKLY_HOURS)
- Warnings: OVERLOAD, QUALIFICATION_EXPIRY, NO_TEMPLATE_MATCH, ABSENCE
- Doppel-Generierung blockiert via UQ_SCHEDULES_WEEK
- DB-Transaction: Komplett-Plan oder Rollback
- GENERATION_METHOD = "FROM_TEMPLATE"
- Frontend: Warnings-Dialog vor Speichern

### Must Not
- Nicht-faellige Leistungen automatisch einplanen (nur als `suggested_entries`)
- Direkt PUBLISHED — immer DRAFT zuerst

## Tasks

### T1: Schedule-Generator-Service (Backend)
### T2: API-Endpoint mit Transaction
### T3: Warnings-Generator-Logic
### T4: Frontend "Woche generieren" + Warnings-Dialog
### T5: Tests Vitest + Playwright (≥10)
### T_last: VERSION 0.4.0 — **MVP fertig**

## Abhängigkeiten

- **Blockiert durch:** TT-16, TT-17, TT-19
- **Blockiert:** TT-21 (Reassignment-Scoring)

## Acceptance Criteria

- [ ] Plan-Generierung für Pilot-Tenant in < 5 Sekunden
- [ ] Alle 4 Warning-Typen werden korrekt erzeugt
- [ ] Doppel-Generierung wird abgelehnt
- [ ] DRAFT-Plan kann manuell editiert + dann PUBLISHED werden
- [ ] Vitest + Playwright: ≥10 Tests grün
- [ ] **MVP-Smoke-Test:** Robert kann Template anlegen, Woche generieren, anpassen, veröffentlichen — Daniel sieht den Plan auf dem Handy
- [ ] spec-gate.sh + doc-version-sync.sh + orphan-check.sh grün
