# Abwesenheit melden — Frontend-Modal (Wave 3)

> **Issue:** ELE-204 | **Erstellt:** 2026-05-17 | **Status:** In Progress
> **Folge von:** ELE-186 ✅ (Backend Auto-Markierung) + ELE-203 ✅ (Picker)

## Agent-Pattern

- [x] **Solo** — Frontend-only, ein Modal + Hook + zwei Trigger.

**Gewähltes Pattern:** Solo
**Begründung:** Klein, frontend-only, ~2-3h.

## Why

Backend kann seit ELE-186 Absencen verarbeiten + Schedule-Entries automatisch markieren. Aber das Frontend hat keinen Trigger dafür. Robert kann den ELE-203-Picker ohne SQL-Eingriff nicht testen, und in Production muss er sowieso Krankmeldungen erfassen können.

## What

Modal `ReportAbsenceModal.tsx` mit Variante-C: 2 Trigger.

1. **Global**: Button im SchedulePage-Header — Mitarbeiter wählbar im Modal
2. **Kontextuell**: Pro Mitarbeiter in `WorkloadSummary` — Mitarbeiter preselected

Modal-Form-Felder:

| Feld        | Required | UI                                  |
| ----------- | -------- | ----------------------------------- |
| employeeId  | ✓        | Select (nur wenn nicht preselected) |
| absenceType | ✓        | Select (5 Typen)                    |
| startDate   | ✓        | date-Input, default heute           |
| endDate     | ✓        | date-Input, default = startDate     |
| notes       | optional | textarea (max 2000)                 |

Submit → POST `/api/absences` → invalidate `scheduleKeys.entries(scheduleId)` → Cards werden rot.

## Constraints

### Must

- AbsenceTypes: SICK / VACATION / PERSONAL / TRAINING / OTHER
- Default-Datum: heute (Von + Bis)
- Validation: `startDate <= endDate` clientseitig + servervalidiert
- TanStack-Query-Mutation, `useReportAbsence()`
- Erfolg → Modal schließen + Toast oder Inline-Confirm
- Auth-Gate für Trigger: ADMIN/PLANNER/FOREMAN nur
- i18n (en + de) — neuer Namespace `absences`
- A11y: role=dialog, ESC, Backdrop-Click, autoFocus auf erstem Feld
- TypeScript strict

### Must Not

- Kein neuer Backend-Endpoint
- Keine Bulk-Eingabe
- Keine Edit/Delete-Workflows
- Keine direkten Schedule-Updates im Frontend (Backend macht das)

### Out of Scope

- Liste der bestehenden Absences (eigenes Folge-Issue)
- Self-Service-Sick-Report für EMPLOYEE (eigenes Folge-Issue, Backend unterstützt's bereits)
- Drag-Datum-Picker (Range-Picker)
- Halbtags-Granularität (`isFullDay=false` + startTime/endTime)

## Current State

- `backend/src/routes/absences.ts` — POST `/api/absences` schreibt + markiert REASSIGNMENT_NEEDED auf affected entries
- `backend/src/schemas/absences.ts` — `createAbsenceSchema` (employeeId, absenceType, startDate, endDate, isFullDay?, startTime?, endTime?, notes?)
- `ABSENCE_TYPES = ['SICK','VACATION','PERSONAL','TRAINING','OTHER']`
- `frontend/src/api/schedule.ts` — bestehende `useEmployees()` Query + `scheduleKeys.entries()` zum Invalidieren
- `frontend/src/components/WorkloadSummary.tsx` — Per-Employee-Row, hier kommt der kontextuelle Button rein
- `frontend/src/pages/SchedulePage.tsx` — Header bekommt globalen Button

## Tasks

### T1: API-Hook + Types

`frontend/src/api/absences.ts`:

- `AbsenceType` Union
- `CreateAbsenceInput`
- `useReportAbsence()` Mutation mit `invalidateQueries(scheduleKeys.entries(scheduleId))`

### T2: Modal-Komponente

`frontend/src/components/ReportAbsenceModal.tsx`:

- Props: `isOpen`, `onClose`, `defaultEmployeeId?`, `employees`, `scheduleId`
- Form-State + Submit + Error-Handling
- a11y + ESC + Backdrop

### T3: Global-Trigger

In `SchedulePage` Header: Button "Krankmeldung melden" (nur ADMIN/PLANNER/FOREMAN).

### T4: Kontextueller Trigger

In `WorkloadSummary` pro Mitarbeiter-Zeile: kleiner Button (gleicher Role-Gate). Übergibt `defaultEmployeeId`.

### T5: i18n + Tests

- `frontend/src/locales/{en,de}/absences.json`
- Vitest: Modal-Form rendert, Submit triggert API
- Playwright: Admin klickt → Modal → Send → Cards werden rot

### T_last: VERSION 0.6.2 + Doku + Spec-Zusammenfassung

## Acceptance Criteria

- [ ] Global-Trigger im SchedulePage-Header sichtbar (ADMIN/PLANNER/FOREMAN)
- [ ] Kontextueller Trigger in WorkloadSummary pro Mitarbeiter
- [ ] Modal-Form korrekt rendert, Submit funktioniert
- [ ] Erfolgreicher Submit → Schedule-Entries Query invalidiert → Cards rot
- [ ] Vitest grün
- [ ] Playwright E2E grün
- [ ] spec-gate + doc-version-sync + orphan-check grün

## Zusammenfassung

_Wird bei T_last befüllt._
