# JWT-Secret-Rotation-Strategie (Pre-Pilot)

> **Issue:** ELE-188 | **Erstellt:** 2026-05-17 | **Status:** In Progress

## Agent-Pattern

- [x] **Solo** — Backend Multi-Secret-Support + Rotation-Script + ADR + Doku.

**Gewähltes Pattern:** Solo
**Begründung:** Klar abgegrenzt, ~2-3h, keine Cross-Layer-Recherche.

## Why

Bei JWT-Secret-Leak gibt es aktuell **kein dokumentiertes Verfahren**. `signJwt`/`verifyJwt` lesen `process.env.JWT_SECRET` — wenn der kompromittiert ist, gibt es nur die Brechstange: alle User sofort ausloggen (Total-Logout, alle Sessions weg, schlechte UX). Vor Pilot mit echten Mitarbeitern muss eine **planmäßige Rotation** und ein **Notfall-Playbook** stehen. Architecture-Review 2026-05-16 hat das als Tech-Debt-Punkt A markiert.

## What

**ADR-18 + Multi-Secret-Verify + Rotation-Skript + Operator-Playbook.**

> Hinweis Nummerierung: Issue-Beschreibung sagt "ADR-16", aber ADR-16 ist bereits i18n-Strategy und ADR-17 ist ELE-189 Error-Tracking. Wir nutzen **ADR-18**.

Konkretes Deliverable:

1. **ADR-18 `docs/ADR-18-jwt-secret-rotation.md`** — Multi-Secret-Strategie + Rotation-Playbook
2. **Backend** akzeptiert beim Verify zwei Secrets:
   - `JWT_SECRET` = primary (Sign + Verify)
   - `JWT_SECRET_PREVIOUS` = nur Verify (Übergangszeit ~7 Tage)
   - Verify-Logik: erst primary, bei Fehler vorheriger
   - Sign nutzt **immer** nur primary
3. **Rotation-Script** `scripts/rotate-jwt-secret.mjs`:
   - `--dry-run` zeigt Plan ohne Änderung
   - `--apply` ersetzt `.env`:
     - Backup `.env` → `.env.<timestamp>.bak`
     - Schreibt `JWT_SECRET_PREVIOUS=<alter Wert>`
     - Generiert neues `JWT_SECRET` (48 Bytes via `crypto.randomBytes(48).toString('base64')`)
   - Hinweis im Output: "Restart backend, dann nach 7 Tagen JWT_SECRET_PREVIOUS entfernen"
4. **Operator-Playbook** in `GOVERNANCE.md` Sektion "JWT-Secret-Rotation":
   - Geplante Rotation (alle 90 Tage)
   - Notfall-Rotation bei Verdacht (sofort)
   - Cleanup nach 7 Tagen
5. **Tests** Vitest: signJwt mit primary, verifyJwt mit primary, verifyJwt mit previous (= rotation simuliert), verifyJwt fail bei drittem Secret

## Constraints

### Must

- `signJwt` nutzt **immer** das primary Secret — niemals previous
- `verifyJwt` versucht primary zuerst, dann previous
- Beide Secrets müssen `SECURITY.JWT_SECRET_MIN_LENGTH` (64 chars) erfüllen
- Rotation-Script ist **idempotent**: zweimal `--apply` rotiert zweimal (kein "rotiert schon vor 10 Sekunden")
- `.env`-Backup vor jeder Rotation
- TypeScript strict, kein `any`
- Tests funktionieren in CI (kein Secret-Leak in Logs)

### Must Not

- Keine automatische Rotation per Cron (Out of Scope, manuell reicht für Pilot)
- Kein HSM/Vault (Out of Scope, Welle 6+)
- Keine Änderung am JWT-Payload-Format
- Kein Secret im Git committen

### Out of Scope

- Automatische Rotation per Cron
- HSM / Vault-Integration
- Forced-Logout-Endpoint — Operator-Playbook gibt eine SQL-Variante ("update users set must_change_password=true") als manuelle Notbremse

## Current State

**Relevante Dateien:**

- `backend/src/auth/jwt.ts:31-69` — `getSecret()`, `signJwt`, `verifyJwt`
- `backend/src/config.ts:69,77,86` — `env.JWT_SECRET`, `validateEnv()`, Min-Length-Check
- `backend/src/auth/middleware.ts:30` — Aufruf von `verifyJwt`
- `lib/config.js SECURITY.JWT_SECRET_MIN_LENGTH = 64` — Validation-Konstante
- `.env.example` — JWT_SECRET steht drin, JWT_SECRET_PREVIOUS muss ergänzt werden
- `GOVERNANCE.md` — Operator-Playbook ergänzen

**Bestehende Patterns:**

- `getSecret()` liest zur Laufzeit aus `process.env` (Tests können überschreiben) — wir behalten das
- `validateEnv()` ist die Pflicht-Validation für ENV-Variablen

**Architektur-Dimensionen (betroffen):**

- **Security** — Kern-Thema
- **Reliability** — Rotation ohne User-Logout (verify-fallback erlaubt 7d Übergang)
- **Maintainability** — Operator-Playbook reduziert Bus-Faktor

## Tasks

### T1: ADR-18 + GOVERNANCE.md-Sektion

- `docs/ADR-18-jwt-secret-rotation.md`
- `GOVERNANCE.md`: neue Sektion "JWT-Secret-Rotation"
- Verify: Markdown rendert, ADR + GOVERNANCE referenzieren sich

### T2: Backend Multi-Secret-Verify

- `backend/src/auth/jwt.ts` — `verifyJwt` versucht primary, dann previous
- `backend/src/config.ts` — `validateEnv()` validiert Length wenn `JWT_SECRET_PREVIOUS` gesetzt
- `.env.example` — `JWT_SECRET_PREVIOUS=` ergänzen
- Verify: Typecheck grün, vorhandene Tests bleiben grün

### T3: Rotation-Script

- `scripts/rotate-jwt-secret.mjs` mit `--dry-run` / `--apply`
- Verify: `--dry-run` zeigt Plan ohne `.env` zu ändern; `--apply` schreibt neu mit Backup

### T4: Tests Vitest

- `backend/tests/auth/jwt-rotation.test.ts`:
  - sign + verify mit primary funktioniert
  - sign mit primary, dann verify mit primary (alte Tokens-Pfad)
  - sign mit OLD primary, dann ROTATION: new primary + old=previous → old-Token verifiziert über previous
  - drittes Secret → Verify schlägt fehl
- Verify: alle grün

### T_last: Doku + VERSION 0.5.5 + Spec-Zusammenfassung

## Dokumentations-Impact

| Datei                    | Was ändern                            |
| ------------------------ | ------------------------------------- |
| `docs/ADR-18-*.md`       | Neu                                   |
| `GOVERNANCE.md`          | Operator-Playbook ergänzen            |
| `ARCHITECTURE_DESIGN.md` | §9 Referenzen + ADR-18 in ADR-Tabelle |
| `CHANGELOG.md`           | v0.5.5-Eintrag                        |
| `CLAUDE.md`              | Version + Stand                       |
| `.env.example`           | `JWT_SECRET_PREVIOUS=`                |

## Abhängigkeiten

- **Blockiert durch:** ELE-169 ✅, ELE-170 ✅
- **Blockiert:** Pilot-Launch (zusammen mit ELE-187 ✅ + ELE-189 ✅ + ELE-191)

## Acceptance Criteria (aus Issue)

- [ ] ADR-18 angelegt (Issue schreibt "ADR-16" — outdated, siehe Hinweis oben)
- [ ] Backend akzeptiert beide Secrets während Übergang
- [ ] Rotation-Skript getestet (Dry-Run + Apply)
- [ ] Doku im GOVERNANCE.md: Operator-Schritte bei Verdacht / planmäßiger Rotation
- [ ] Vitest: Test dass altes + neues Secret beide funktionieren
- [ ] spec-gate + doc-version-sync + orphan-check grün

## Zusammenfassung

_Wird bei T_last befüllt._
